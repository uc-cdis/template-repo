[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
pytest = "~=3.2"
pytest-cov = "~=2.5"

[packages]

[requires]
python_version = "{{ cookiecutter.python_version }}"
